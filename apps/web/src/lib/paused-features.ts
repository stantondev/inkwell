// Postage (reader tips) and writer subscription plans needed Stripe Connect,
// which went away with the Stripe account. Their code is kept for a possible
// return; these flags keep them out of the interface until then.
export const POSTAGE_ENABLED = false;
