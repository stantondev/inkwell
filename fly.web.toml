# ──────────────────────────────────────────────────────────────
# Fly.io config for Inkwell Next.js Frontend
# Deploy with: fly deploy --config fly.web.toml
# ──────────────────────────────────────────────────────────────

app = "inkwell-web"
primary_region = "ord"

[build]
  dockerfile = "apps/web/Dockerfile"
  [build.args]
    NEXT_PUBLIC_API_URL = "https://api.inkwell.social"
    NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY = "pk_live_51T3CeT3leE4x2l02n5uISZn0n98IEvZZcv3vsDtVCY1CvtlhzYtqvabTLFGtE9Zete3xGHtRzBMTdyXiOsAr4pOf00PxcT3dH1"

[env]
  PORT = "3000"
  NODE_ENV = "production"
  API_URL = "https://api.inkwell.social"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = "suspend"
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]

  [http_service.concurrency]
    type = "connections"
    hard_limit = 250
    soft_limit = 200

[[http_service.checks]]
  interval = "30s"
  timeout = "15s"
  grace_period = "90s"
  method = "GET"
  path = "/"

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
